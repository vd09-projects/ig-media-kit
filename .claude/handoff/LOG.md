# Handoff Log

Append-only chronology. Maintained by Skald — one row per skill invocation, newest at bottom.

| Date | Scope | Skill | Event |
|---|---|---|---|
| 2026-07-14 17:50 | fetch-engine-store-foundation | mimir | planner-task v1 (draft) — T1 fetch engine + store foundation |
| 2026-07-14 17:54 | fetch-engine-store-foundation | multi-perspective-review | review-findings-plan v1 (draft) — plan review round 1, REQUEST CHANGES, 4 blockers |
| 2026-07-14 18:01 | fetch-engine-store-foundation | mimir | planner-task v2 (draft) — revised plan, all 4 round-1 blockers resolved |
| 2026-07-14 18:12 | fetch-engine-store-foundation | multi-perspective-review | review-findings-plan v2 (draft) — plan review round 2, REQUEST CHANGES, all 4 prior blockers resolved, 1 NEW blocker (top_scan stop-condition on shortcodes) |
| 2026-07-15 07:23 | fetch-engine-store-foundation | mimir | planner-task v3 (draft) — round-2 blocker resolved: top_scan stop condition redefined as seen-set membership + numeric high_water_media_id watermark, pages_fetched==1 anti-regression check added |
| 2026-07-15 07:35 | fetch-engine-store-foundation | multi-perspective-review | review-findings-plan v3 (draft) — plan review round 3 (final), APPROVE, round-2 blocker genuinely resolved (seen-set membership + numeric high_water_media_id watermark + pages_fetched==1 anti-regression check), no new blockers, sound enough to build |
| 2026-07-15 07:44 | fetch-engine-store-foundation | sindri | implementation-build v1 (draft) — built T1 foundation (config/http+stop_signal/fetch+top_scan/CSV+YAML store durable-first/FastMCP skeleton); 43 offline tests pass; T1.2 probe ran LIVE (confirmed shapes + captured a real 401 stop_signal); flagged pinned-reel non-newest-first ordering as a followup |
| 2026-07-15 13:20 | fetch-engine-store-foundation | multi-perspective-review | review-findings v1 (draft) — BUILD review round 1, APPROVE, 0 blockers; 4 suggestions (transport-exception/timeout handling, 200-challenge-with-items durable-persist, distinct 5xx reason, dead imports) + 1 accepted-debt (pinned-reel under-collection hardening, safe as shipped) |
| 2026-07-15 14:22 | t2-list-reels-discovery-ranking | mimir | planner-task v1 (draft) — T2 list_reels full Flow A: serve-from-store fast path, top-check + deepen two-phase call-driven fill toward scan_depth=90 under one ~4-page budget, coverage-segment gap tracking, partial-on-stop_signal, rank-over-pool; 11 ordered steps, no downloads |
| 2026-07-15 14:30 | t2-list-reels-discovery-ranking | multi-perspective-review | review-findings-plan v1 (draft) — plan review round 1, REQUEST CHANGES, 4 blockers (B1 pinned-reel top-check short-circuit skips newer reels; B2 B1 collides with T1-freeze, needs pinned-account micro-gate; B3 high_water_media_id bump must be numeric-max not positional-top; B4 pool_depth count vs coverage-contiguity conflation) + 4 suggestions |
| 2026-07-15 20:10 | t2-list-reels-discovery-ranking | mimir | planner-task v2 (draft) — revised plan, all 4 round-1 blockers resolved (T2.4a pinned-prefix carve-out for B1/B2, numeric-max high_water for B3, contiguity-gated serve-from-store for B4) |
| 2026-07-15 20:15 | t2-list-reels-discovery-ranking | multi-perspective-review | review-findings-plan v2 (draft) — plan review round 2 (targeted, 3 blocking reviewers), APPROVE, all 4 prior blockers (B1/B2/B3/B4) genuinely resolved, 0 new blockers, T2.4a budget-burn risk contained; 3 non-blocking suggestions |
| 2026-07-15 17:21 | t2-list-reels-discovery-ranking | multi-perspective-review | review-findings v1 (draft) — BUILD review round 1, APPROVE, 0 blockers; all 5 hard invariants verified in code + test-locked (anonymous-only, never-sleeps/≤4-page/partial-on-stop, bounded T2.4a TOP_SCAN-only carve-out with deep_resume untouched + caught_up≠page_cap, contiguity-gated non-destructive store + monotonic high_water, durable-first); modified T1 test judged legitimate (not masking); unobserved live T2.10 happy-path acceptable as non-blocking follow-up; 90 offline tests pass; 2 suggestions + 2 FYI |
| 2026-07-15 17:31 | t2-list-reels-discovery-ranking | implementation | build | v1 | draft | comment-only backfill of the two T2 followup markers with real issue numbers (window.py run_window/compose-drift -> #8; fetch.py PINNED_PREFIX_BOUND under-collect -> #9); zero logic change, pytest 90 passed; first persist of the T2 implementation-build canonical |
| 2026-07-15 19:25 | t3-download-reel-signed-url-refresh | mimir | planner-task | v1 | draft | T3 download_reel task breakdown (T3.1–T3.8): shortcode→handle+row resolver, strict no-network cached-hit gate, TTL-margin freshness decision, shortcode/media_id-anchored owner-feed re-resolve (never positional), binary redirect-follow CDN download + ftyp-verify, atomic local_mp4/video_url/fetched_at manifest rewrite, stub→real MCP wiring, unit suite + live pilot; 3 open questions (TTL margin, refreshed-URL persistence, unknown-shortcode policy) |
| 2026-07-15 19:32 | t3-download-reel-signed-url-refresh | multi-perspective-review | review-findings-plan v1 (draft) — plan review round 1, APPROVE, 0 blockers; two load-bearing anchors (non-positional shortcode/media_id re-resolve; provably network-free cached path) verified against T1/T2 code; anonymity holds on both new network paths; atomic CSV rewrite mirrors _write_state_atomic; 1 should-fix (ftyp tag at offset 4 not byte 0) + 5 suggestions/4 test additions; all 3 open questions resolved (OQ#2 safe+correct — nothing reads fetched_at; OQ#3 forced to typed-error — per-media endpoint dead anonymously) |
| 2026-07-16 01:20 | t3-download-reel-signed-url-refresh | implementation | build | v1 | draft | T3 download_reel built (T3.1–T3.8) on feat/t3-download-reel + ftyp offset-4 fix folded in: download_cdn binary redirect-follow GET + CdnResponse (http_client); find_reel + atomic update_local_mp4 (store); resolve_reel_url identity-anchored never-positional re-resolve + ResolveResult (fetch); new download.py run_download_reel orchestration (cached no-network gate, 24h TTL-margin freshness, reuse-or-re-resolve, ftyp-verify+temp+os.replace, atomic manifest, typed envelopes); mcp_server stub→real with try/except backstop. 16 new tests incl. provably-network-free cached path + not-first-in-feed standing-order guard; pytest 106 passed; no lint/type tooling configured. Live pilot probe/probe_download.py written, NOT run (would hit IG). 1 discovered followup (# TODO in download.py: fall back to one re-resolve when a reused in-margin URL fails to download). Recommends multi-perspective-review |
| 2026-07-16 23:20 | t4-async-batch-runner | review | findings | v3 | draft | BUILD review round 1 — REQUEST CHANGES; 1 blocker F1 (note_metered_stop runs AFTER gate.acquire() releases → under multi-worker resume/overlap a 2nd job can fire a window on the just-401'd IP before the escalated cooldown lands, breaching back-off-under-abuse; fix = set cooldown inside the gate critical section); anonymity hard-stop PASSES (IG via AnonymousClient, callback bare no-header/no-cookie transport, redirects off, DNS-pinned); SSRF guard comprehensive; FetchGate FIFO mutual-exclusion correct (max_in_flight==1); resume idempotent+durable; store additive-only; 37 tests offline fake-clock zero-sleep. Non-blocking: F6 two-worker race test, F2 dead-worker masked as cooldown-sleeping, F7 run_window divergence, F5 retries bounds hard-block wait, F4 IPv6 pin FYI. Corroborated: F1 (Reliability+Concurrency) |
<!-- Skald appends rows here as skills run. -->
| 2026-07-16 01:40 | t3-download-reel-signed-url-refresh | review | findings | v1 | draft | APPROVE — 0 blocking; 3 non-blocking suggestions (numeric-backstop test gap, TODO fallback, aged-out envelope semantics) |
| 2026-07-16 01:45 | t3-download-reel-signed-url-refresh | implementation | build | v2 | draft | iterate (surgical): (A) numeric media_id backstop test now uses differing code + same pk so only the backstop can match; (B) aged-out/not-found-in-budget envelope drops partial=True → clean non-partial typed error, distinct from stop_signal partial (retryable); reused-in-margin 403 TODO left as-is; pytest 106 passed |
| 2026-07-16 03:18 | t4-async-batch-runner | planner | task | v1 | draft | T4 async batch runner breakdown (concurrency overlay), 9 ordered steps on shipped T2/T3 reuse: BatchJob/JobPhase model + atomic store/_batch/<job_id>.json checkpoint (reuses _write_state_atomic); serialized per-handle fill loop around run_window that sleeps ≥1 cooldown window on metered stop_signal + resumes from persisted cursor (only sleep site, never polls in cooldown); strictly-sequential whole-job driver (single-IP invariant); global/per_channel aggregation via ranking.load_pool/filter_pool/rank/select_top over full deduped/watermarked pools; optional download_top via run_download_reel; callback poster on bare non-IG transport (no x-ig-app-id/cookies) with bounded exp-backoff retry, result durability decoupled from delivery; instant-return start_batch_fetch + pure-read get_batch_status. Concurrency overlay: shared-state inventory, 5 invariants, 6-row crash/recovery table, at-least-once callback idempotency keyed on job_id, fault-injection + fake-clock race strategy. 5 open questions (exec mechanism, cross-job serialization, callback URL SSRF policy, batch config shape, checkpoint format) |
| 2026-07-16 21:56 | t4-async-batch-runner | review | findings | v1 | draft | plan review round 1 — REQUEST CHANGES; 3 blockers (cross-job fetch serialization left as Open Question = parallel-IG politeness-counter-metric risk; no resume entrypoint for existing job_id vs restart-resume being the primary success metric; caller-supplied callback URL SSRF + redirect-follow unaddressed); 5 suggestions; approve-grade on failure-mode table, checkpoint atomicity, resume idempotency, callback anonymity/durability decoupling |
| 2026-07-16 22:15 | t4-async-batch-runner | planner | task | v2 | draft | round-1 review-response revision (prior v1 archived) — all 3 blockers resolved in-design not deferred: (B1) cross-job/cross-tool serialization promoted to a hard invariant + foundational Step 3 process-wide FetchGate singleton (second start queues behind, carries shared cooldown-until + escalation counter; T4 ships it, T5 wraps sync list_reels/download); (B2) resume_pending_jobs(config) added as first-class Step 9 entrypoint — idempotent store/_batch sweep relaunching non-terminal jobs from checkpoint on fresh daemon threads with per-job_id launch guard, called on T5 startup + at module init; (B3) callback SSRF resolved Step 8 — https-only, DNS-resolve+block private/link-local/loopback/metadata, redirect-follow disabled, re-checked pre-POST (DNS-rebind guard), bare non-IG transport. Folded suggestions: escalation-sourced cooldown sleep w/ decay, get_batch_status fetching/cooldown/dead-worker liveness, one stable handle-keyed result envelope (global under \"*\"), *.tmp sweep, pinned param contract, new serialization/restart/SSRF/unknown-id tests. Open Questions narrowed 5→2 (cooldown-curve constants + heartbeat_stale_s), both probe-tunable + non-blocking |
| 2026-07-16 22:40 | t4-async-batch-runner | review | findings | v2 | draft | plan review round 2 — APPROVE, 0 blockers; all 3 round-1 blockers verified resolved in revised design (FetchGate Step 3 cross-job serialization; resume_pending_jobs Step 9 restart-resume; Step 8 callback SSRF https+range-block+no-redirect+pre-POST re-resolve); Open Questions 5→2 (cooldown constants, heartbeat_stale_s), non-blocking; 4 non-blocking hardening suggestions (in-memory gate cooldown resets on restart; import-side-effect resume; unfair Lock vs FIFO; DNS-rebind TOCTOU IP-pinning) |
| 2026-07-16 22:45 | t4-async-batch-runner | implementation | build | v1 | draft | T4 async batch subsystem built (planner-task v2, all 10 steps) on modification-free T2/T3 reuse: new fetch_gate.py (process-wide FIFO FetchGate singleton, persisted+escalating cooldown, sleep-never-poll — the only sleeper); new batch.py (BatchJob/JobPhase/HandleProgress model + stable result envelope, _fill_handle/_run_job phase machine, _aggregate global|per_channel over full deduped pool, _download_top via T3, _post_callback + validate_callback_url SSRF guard, run_start_batch_fetch instant job_id + detached daemon, run_get_batch_status pure checkpoint read, resume_pending_jobs restart re-adopt, BatchDeps injection); additive store.py batch-job/result JSON persistence (atomic temp+fsync+os.replace, no existing method changed); additive config.py BatchSettings; mcp_server wired batch_fetch + get_batch_status. 4 required refinements landed (persisted cooldown, explicit non-import resume, FIFO fairness via Condition+ticket queue, DNS-rebind TOCTOU IP-pinning). Anonymous-only + politeness invariants honored; all tests injected transports + fake clock (zero real sleeps/network). pytest 143 passed (106 baseline + 37 new), 0 failed. 1 discovered follow-up (reconcile batch per-window primitive with run_window: run_window is TOP_SCAN-only so _fill_handle loops run_list_reels; retire or extend run_window). Recommends multi-perspective-review |
| 2026-07-16 23:45 | t4-async-batch-runner | implementation | build | v2 | draft | iterate on round-1 build review (findings v3) — prior v1 archived to _history/implementation-build-v1.md. F1 (BLOCKING) fixed: metered/clean classification + gate mutation (note_metered_stop/note_success) moved INSIDE _fill_handle's `with gate.acquire()` critical section so the escalated cooldown lands before the gate releases — a concurrent worker can no longer open a window on the just-401'd IP before the back-off (post-gate bookkeeping/checkpoint unchanged). F2 fixed: _classify_liveness checks _worker_alive before the cooldown clock so a crash mid-cooldown surfaces as dead-worker, not masked as cooldown-sleeping up to cooldown_cap_s. F5: BatchSettings.retries docstring notes it also bounds total hard-block wait (stall guard = retries+2). F4: comment in _default_poster on IPv4-only resolve-map pinning (bracket an IPv6 literal if ever pinned). F7 left as accepted debt — self-describing run_window TODO marker retained, no issue number minted. Tests: +test_two_workers_no_window_opens_after_401_until_cooldown_slept (F6 two-worker fake-clock race guard), +test_liveness_crash_mid_cooldown_is_not_masked_as_sleeping (F2), updated test_liveness_cooldown_vs_dead_worker to register a live worker for the sleeping case. pytest 145 passed, 0 failed. Recommends re-review |
| 2026-07-16 23:55 | t4-async-batch-runner | review | findings | v4 | draft | BUILD review round 2 (iteration) — APPROVE, 0 blockers; prior v3 archived to _history/review-findings-v3.md. Both round-1 findings verified RESOLVED against the diff + a green 145-test offline suite: F1 (BLOCKING) — note_metered_stop/note_success + outcome classification now execute INSIDE _fill_handle's `with gate.acquire()` (batch.py:274–308), so the escalated cooldown is set before release and the next acquirer sleeps it out; no lock-nesting/deadlock (acquire() yields outside _cond). New regression test_two_workers_no_window_opens_after_401_until_cooldown_slept genuinely guards it (two real threads + one shared gate + fake clock; asserts min(B IG-call times) >= NOW+base — fails against pre-fix ordering). F2 — _classify_liveness checks _worker_alive before the cooldown branch (batch.py:787–811) so a crash mid-cooldown reads dead-worker; test_liveness_crash_mid_cooldown_is_not_masked_as_sleeping covers it. F5 docstring accurate. Re-scan found NO new blocking issues: anonymity holds (IG via AnonymousClient, callback bare non-IG), politeness now atomic, dedupe+watermark + SSRF guard untouched, FIFO gate/idempotent resume/daemon lifecycle correct. Remaining: F7 accepted debt (run_window divergence, deferred to T5). Recommends approve + merge |
| 2026-07-16 07:57 | t5-ship-mcp-server-packaging | planner | task | v1 | draft | T5 capstone ship plan (task depth, public-api-change overlay) breaking down assembly of the four merged tools into one runnable/packaged FastMCP server across 6 reviewable slices: (1) shared server context + argparse --config with --config>$IG_MK_CONFIG>default precedence + load_config-once + explicit resume_pending_jobs(config) at startup before mcp.run(); (2) freeze the four-tool public surface — drop stale top_reels stub, rename batch_fetch->start_batch_fetch, add tool-set schema snapshot contract test, preserve download_reel partial/stop_reason vs typed-error partial=False distinction through wiring; (3) product_type dispatch switch at fetch.py:175-198 + one disabled non-clip stub handler (default image) proving localized extension; (4) pyproject/entry-point/$IG_MK_CONFIG/config.yaml verification + clean-venv fresh-clone install pass; (5) README rewrite (install->configure->run, 4 tools + when-to-use, ~48/6.6min escalating politeness, ~36h signed-URL TTL/24h re-resolve margin, Burner rejected linking report section); (6) offline fixture/dry-run smoke harness (list->download->batch->local callback sink, temp store, asserts zero instagram.com) + opt-in-gated live procedure DEFERRED to pilots #10/#14 (not faked green). Honors ANONYMOUS-ONLY, load-bearing politeness, x-ig-app-id header, single FetchGate serialization + ungated CDN, per-shortcode+media_id watermark non-positional standing order. 3 open questions (stub type choice, harness location, $IG_MK_CONFIG precedence existing?), all with safe defaults. First artifact in scope — version 1, no prior |
| 2026-07-16 08:04 | t5-ship-mcp-server-packaging | multi-perspective-review | review-findings v1 (draft) — PLAN review round 1, REQUEST CHANGES, 2 blockers (list_reels missing never-raise envelope; per-call config_path can split the process-wide FetchGate); all 5 ACs covered, live-smoke deferral to pilots #10/#14 endorsed |
| 2026-07-16 08:09 | t5-ship-mcp-server-packaging | planner | task | v2 | draft | ITERATION on plan review round 1 (findings v1, REQUEST CHANGES) — prior planner-task v1 archived to _history/planner-task-v1.md. Added a Review Response section acknowledging all 8 items (2 blocking, 6 non-blocking; none rejected, 2 refined). Blocking-1 (never-raise): step 2 now explicit work to wrap list_reels (mcp_server.py:24-57, no try/except) in the shared typed never-raise envelope; contract test now asserts each of the four tools returns a dict (never raises) when its run_* throws, not just names/params. Blocking-2 (FetchGate split): step 1 now resolves ONE FetchGate from server context, forbids re-keying get_gate off a per-call config_path, and constrains the surviving config_path arg to a store-compatible/test-only override (divergent-store_dir rejected via typed error, not silently gate-split); adds startup-order assertion resume_pending_jobs completes before mcp.run() and daemon shares the SAME gate instance (identity); new Risk + gate-identity/split test. Non-blocking folded: product_type dispatch made observable via named registry + typed SkipReason (both paths were indistinguishable None at fetch.py:182) so routing test is writeable + self-describing follow-up marker (no issue number); simulated mid-fetch 401 driven through the wired surface in the smoke/wiring test; Open Question #3 RESOLVED (config.py:107 already implements explicit>$IG_MK_CONFIG>./config.yaml — step 4 verify-only, main() reuses load_config); resume_pending_jobs documented operator-facing in README; smoke localhost callback sink injected as a test seam (production https/SSRF guard NOT loosened). New Constraints (never-raise surface, one-gate-per-process), new Consumer Inventory row (config_path now same-store_dir), updated Versioning/Contract-Tests/Communication overlay sections. Awaiting re-review |
| 2026-07-16 08:16 | t5-ship-mcp-server-packaging | multi-perspective-review | review-findings-plan v2 (draft) — plan review round 2 (targeted, 2 prior-blocking reviewers + full invariant pass) — APPROVE, 0 blockers; prior findings v1 archived to _history/review-findings-v1.md. Both round-1 blockers verified RESOLVED against live source: (1) never-raise — mcp_server.py:51-56 still un-wrapped confirmed, plan v2 makes the wrap explicit Step 2 work + behavioral dict-return-on-throw contract test across all four; (2) config_path gate split — resolve-once-from-server-context + reject-divergent-store_dir guard closes it. One non-blocking accuracy note: fetch_gate.py:187-215 get_gate is an argument-ignoring module singleton (same instance regardless of args), NOT store_dir-keyed as plan lines 45/69 + CLAUDE.md state — gate cannot literally be split by config_path today and the gate-identity assertion is vacuous; prescribed guard still correct, lean the test on the rejection assertion. All 6 round-1 non-blocking suggestions folded in; all invariants honored (politeness now verified through the wired surface) |
| 2026-07-16 11:21 | t5-ship-mcp-server-packaging | implementation | build | v1 | draft | T5 capstone ship built (planner-task v2, all 5 slices) — assembles the four merged tools into one runnable/packaged/documented FastMCP server. mcp_server.py rewritten: frozen ServerContext (config+Store+one process-wide FetchGate) read by every tool; top_reels stub removed; batch_fetch->start_batch_fetch; list_reels wrapped in the shared never-raise envelope; main() argparse --config REUSING load_config precedence (explicit>$IG_MK_CONFIG>./config.yaml); startup() installs context + calls resume_pending_jobs EXPLICITLY before mcp.run(); one-gate guard rejects a divergent-store_dir config_path as a typed envelope (asserts REJECTION, not object identity — get_gate is an argument-ignoring singleton). fetch.py: observable product_type switch (named _PRODUCT_HANDLERS registry + typed SkipReason + NormalizeResult); disabled image demonstrator stub + self-describing follow-up marker (no minted issue number); normalize_item stays byte-for-byte-compatible; store stays clips-only. config.yaml: added batch: politeness block + documented min_duration. README fully rewritten (install/configure/run, startup job adoption, 4 tools+when-to-use, ~48/6.6min escalating politeness, ~36h signed-URL TTL/24h margin, Burner rejected linking report section, deferred-live/fixture smoke). New probe/probe_smoke.py: offline fixture harness driving list->download->batch+local-callback-sink->status + a mid-fetch 401, under a guard that hard-fails on any real transport (zero IG network enforced); localhost sink injected as a poster test seam (production https/SSRF guard unchanged); live run opt-in-gated + DEFERRED to pilots #10/#14, not faked green. Tests +17 (test_mcp_server, test_product_type_dispatch, test_smoke). pytest 162 passed (145 baseline + 17), deterministic x3; fresh-clone pip install . -> ig-media-kit --help exit 0; server boots with resume-before-serve line. 2 discovered follow-ups (product_type stub seam marker; sync tools don't acquire() the gate — a non-sleeping cooldown check for list_reels/download is a deferred design question). Recommends multi-perspective-review |
| 2026-07-16 11:27 | t5-ship-mcp-server-packaging | multi-perspective-review | review-findings | v3 | draft | BUILD/code review (iteration 1) of implementation-build v1 on feat/t5-ship-mcp-server — APPROVE, 0 blocking; prior plan-review findings v2 archived to _history/review-findings-v2.md. 10-reviewer backend panel. Both round-1 plan blockers confirmed closed in code: list_reels typed never-raise envelope (behavioral all-four-tools test) + one-gate divergent-store_dir REJECTION guard (ContextMismatch->typed envelope, not object-identity). Verified all run_* accept new store=/deps= kwargs, three changed modules byte-compile clean, fetch.py imports resolve. All invariants honored (resume before mcp.run() order-spy, observable product_type switch clips-only + non-positional shuffled watermark, zero-IG-enforced smoke, SSRF guard intact via injected poster seam, live run deferred #10/#14 not faked). One non-blocking tension (Concurrency+Tech Debt corroborated): sync tools share gate object but don't acquire() — can hit IG during batch cooldown; NOT a T5 regression, plan-scoped out, disclosed as follow-up #2; recommend filing it + non-clip-enablement stub as tickets. Caveat: pytest/mcp absent in review sandbox, 162-passed claim not re-executed — findings rest on source analysis + byte-compile |
| 2026-07-18 10:38 | t17-list-reels-readonly | planner | task | v1 | draft | Pivot list_reels to a pure READ-ONLY query over the local store (hard CQRS split, no fetch escape hatch) per the accepted api-contract decision. 9 ordered steps: excise the IG-fetch path from run_list_reels (drop network top-check/_run_deepen/PageBudget/fresh_fetch + AnonymousClient/fetch_window/resolve_user_id imports); a three-state readiness branch (not-analyzed -> typed error; analyzed-shallow/stale -> serve+staleness with complete=False; analyzed-contiguous -> serve clean) keyed on coverage.is_contiguous with a _has_been_analyzed gate that is NOT the scan_depth count; typed not-analyzed error modeled on download.py::_error(partial=False, retryable=False, error_kind naming start_batch_fetch); staleness block (last_analyzed_at, store_count-vs-90 informational, signed_url_maybe_expired ~36h TTL); D1 decision on last_analyzed_at sourcing (recommend a State field stamped in write_window over deriving max(fetched_at)); unwire from FetchGate + drop fresh_fetch param; deliberate frozen-snapshot contract update; tests incl. the zero-IG-request assertion for both cases; README+CLAUDE.md docs. public-api-change overlay active (breaking: param removal + response-schema staleness block). 3 open questions (D1 sourcing, error_kind discriminator, signed_url scope). Awaiting approval |
| 2026-07-18 10:45 | t17-list-reels-readonly | review | findings | v1 | draft | PLAN review iteration 1 — APPROVE, 0 blockers; 10-reviewer backend panel. All six scrutiny axes verified against live source: (a) total IG-fetch excision no escape hatch (grep + zero-IG test); (b) readiness gate = coverage-contiguity via _has_been_analyzed, separated from informational store-count-vs-90 (checked vs is_contiguous terminal-or->=scan_depth); (c) not-analyzed error rides never-raise envelope partial=False; (d) ranking reused unchanged, per-shortcode dedupe + numeric media_id never positional; (e) zero-IG-request assertion both cases; (f) README+CLAUDE.md updates present. 5 non-blocking suggestions: (1) download._error has NO retryable/error_kind fields (retryability via partial) -> plan's new keys create contract asymmetry, resolve Open Q#2 as hard AC; (2) pin staleness present-on-success/absent-on-error for deterministic snapshot; (3) add _compose_note to Step 1 deletion list (goes partly dead); (4) resolve D1 before Step 8 staleness tests; (5) assert not-analyzed error path records 0 network calls. Sound to build |
| 2026-07-18 12:39 | t17-list-reels-readonly | implementation | build | v1 | draft | list_reels pivoted to a pure READ-ONLY query over the store (run_list_reels: resolve->validate->load state->three-state readiness via _has_been_analyzed keyed on coverage evidence not scan_depth count->{typed not_analyzed error naming start_batch_fetch | served ranked top-N + deterministic staleness block}); ZERO IG on every path (poisoned-transport test asserts 0 calls on BOTH served + not-analyzed). FORCED DEVIATION: batch._fill_handle reused run_list_reels as its only fetch engine, so the network path (top-check+deepen+PageBudget+_run_deepen+_compose_note) was RELOCATED intact into new fill.py::run_fill (command-side primitive) and batch re-pointed to it (fetch logic byte-identical, test_batch unchanged) — the plan's own >=2-module re-plan trigger; recommend reviewer ratify as within CQRS intent. D1: State.last_analyzed_at stamped in write_window (now threaded through), legacy YAML loads None. D2: uniform error_kind/retryable on list_reels errors (not_analyzed/invalid_params/internal_error) + backfilled onto download _error (not_in_store/aged_out/download_failed, retryable=False) and _partial (rate_limited, retryable=True). D3: signed_url_maybe_expired over served rows only. mcp_server drops fresh_fetch param (frozen-surface deliberate change); README+CLAUDE.md metered-paths now name only start_batch_fetch + download re-resolve. Tests: test_list_reels rewritten, new test_fill (migrated network-path coverage), test_mcp_server surface+response-contract, test_download error-fields. 177 pytest passed; byte-compile clean (no ruff/mypy in env); offline probe_smoke exit 0. 2 follow-ups: (1) _Params/_resolve_params/_validate duplicated list_reels<->fill (# DUP marker) -> extract params.py; (2) window.run_window now fully dead. Ready for review — recommend multi-perspective-review |
| 2026-07-18 12:48 | t17-list-reels-readonly | multi-perspective-review | review-findings | v2 | draft | CODE review (iteration 1) of implementation-build v1 on feat/t17-list-reels-readonly — APPROVE, 0 blocking; prior PLAN-review findings v1 archived to _history/review-findings-v1.md. 12-reviewer backend panel; reviewer independently re-ran full suite (177 passed, 1.32s) + affected subset (84 passed) and verified all seven axes by source inspection + grep, not from the build note. FORCED DEVIATION explicitly RATIFIED: fill.py::run_fill is a verbatim lift of the old run_list_reels network path (PageBudget cap, reserve-deepen, sleep=None both fetch_window calls, first-stop-aborts-whole-unit); only non-verbatim delta is additive now-threading into write_window for last_analyzed_at (no fetch-semantic change); batch._fill_handle re-pointed, test_batch green, envelope contract preserved. Verified: (a) zero-IG on BOTH served + not-analyzed paths — _poison_network patches http_client._default_transport, AnonymousClient.__init__ builds it when no transport injected, both tests assert hits==0, not-analyzed asserted specifically; list_reels imports neither .fetch nor .http_client. (b) fill extraction behavior-preserving + list_reels references no fetch symbol. (c) three-state gate via _has_been_analyzed = pool>0 OR segments OR high_water, scan_depth never in the gate; complete = coverage.is_contiguous separately; store_count-vs-90 informational only (test proves complete stays True at count 1<<90); segments-present/empty-pool edge serves empty not errors. (d) uniform error_kind/retryable on list_reels (not_analyzed/invalid_params/internal_error, retryable=False) + download backfill (_error not_in_store/aged_out/download_failed retryable=False; _partial rate_limited retryable=True); error_kind now keyword-only, all 3 call sites updated, module-private no external break; both frozen snapshots updated with auditable comments. (e) rank() desc over full deduped pool (load_pool, no cap), shortcode tiebreak, non-positional — test_analyzed_serve_ranks_over_deduped_pool_not_positional. (f) staleness always-on-served / always-absent-on-error (asserted 3 ways); signed_url_maybe_expired = min(served fetched_at) vs 36h TTL, None when unknown; last_analyzed_at new State field stamped every write_window incl empty window, _as_int backward-compat None. (g) README + CLAUDE.md rewritten accurately; anonymous-only structurally reinforced (x-ig-app-id sole-owned by AnonymousClient, no auth header ever set). 4 non-blocking suggestions: (1) TOP — add static/AST test asserting list_reels imports neither ig_media_kit.fetch nor ig_media_kit.http_client (grep-clean is build-time only; two poison tests each hit one branch); (2) extract DUP _Params/_resolve_params/_validate to params.py (follow-up #1); (3) retire dead window.run_window (follow-up #2); (4) fix stale fetch_gate.py:7 docstring + note fresh_fetch removal / vestigial pages_fetched in release notes. Corroborated: fill extraction ratifiable reached independently by Reliability + Ripple + Domain. No timeline gate — none blocks merge |
| 2026-07-18 12:56 | t17-list-reels-readonly | implementation | build | v2 | draft | ITERATION on the APPROVED build (review v2, 0 blocking) — absorbed EXACTLY the two low-risk hardening suggestions, no production behavior change; prior build v1 archived to _history/implementation-build-v1.md. (1) Test Coverage top suggestion — machine-enforce zero-IG import boundary: new tests/test_import_boundary.py, a STATIC AST guard that resolves list_reels via find_spec (imports only parent pkg, never executes the module), walks the whole module (top-level + nested/lazy imports) and asserts it imports neither ig_media_kit.fetch nor ig_media_kit.http_client and no AnonymousClient/fetch_window/resolve_user_id symbol — converts build-time grep into a permanent CI tripwire catching a fetch import on an untested third branch (the gap the two single-branch runtime poison tests leave); single test with an in-function positive control against fill.py (known violator) proving the detector fires, so suite grows by exactly one (177->178). (2) Docs nit — fetch_gate.py:7 module docstring run_list_reels -> run_fill (batch path name post-extraction; docstring-only). Deferred as separate tickets, NOT done: DUP params.py extraction (#1), dead window.py retire (#2). Gate: pytest 178 passed; byte-compile exit 0; offline probe_smoke exit 0 with zero_ig_network True. Ready for review — no new blocking surface |
