# Thread — fetch-engine-store-foundation

## 2026-07-14 17:50 — planner task v1 (draft)

Task-level breakdown for the foundational plumbing all four MCP tools depend on: an anonymous curl_cffi fetch primitive, a persistent CSV+YAML store with skip-seen dedupe and cursor resume, load-bearing politeness baked into the fetch loop, a config schema mirroring yt-media-kit, and a bootable FastMCP skeleton.

## 2026-07-14 18:01 — planner task v2 (draft)

Revised T1 plan against round-1 review. Resolves all four blockers: (1) a `stop_signal` classifier (T1.2 enumerates the real throttle/block/challenge family; T1.3/T1.5 stop on any of it, not just literal 401, failing closed on unknowns); (2) durable-first partial persistence — CSV rows fsync'd before cursor advance, cursor advances only over persisted items, state.yaml written via temp-file + os.replace, with an injected-failure acceptance check; (3) ANONYMOUS redefined to key off auth cookies (sessionid/ds_user_id) rather than "no cookie jar," benign anonymous cookies permitted; (4) two explicit traversal modes with disambiguated state (high_water_id for top_scan, deep_cursor for deep_resume). One scope disagreement noted: T1 ships the top_scan caller and only SUPPORTS deep_resume in the primitive, deferring the deep-backfill caller to a follow-up.

## 2026-07-14 17:54 — review findings v1 (draft)

Multi-perspective panel review of the T1 PLAN (plan review, round 1). Verdict: REQUEST CHANGES. Plan is strong and near build-ready, but four design questions must be resolved before code because each silently breaks a load-bearing invariant if guessed wrong: (B1) stop-signal scoped to literal 401 vs IG's wider throttle/block family — biggest risk to "never poll during cooldown"; (B2) partial-write atomicity/cursor-ordering unspecified — can permanently skip reels, violating "never destructively capped"; (B3) "no cookie jar" over-broad — may break fetch or under-define the anonymous invariant; (B4) new-posts vs deep-paging traversal conflated. Plus: enumerate AC1–AC6, adopt probe-fixture testing, drop the conditional TSV fork.

## 2026-07-14 18:12 — review findings v2 (draft)

Round-2 panel review of the REVISED T1 PLAN, weighted toward the four reviewers who blocked last round. Verdict: REQUEST CHANGES. All four round-1 blockers verified GENUINELY resolved (not name-checked): B1 stop_signal classifier with fail-closed `stop(unknown)` default + probe enumeration; B2 durable-first ordering with an injected-failure acceptance test; B3 auth-cookie-keyed ANONYMOUS with a positive outgoing-request test; B4 disambiguated traversal modes — and the scoping decision (ship top_scan caller, support-only deep_resume, defer deep-backfill) assessed SOUND. The single REQUEST CHANGES is driven by ONE new blocker the revision introduced: T1.5's concrete top_scan stop condition is written as an ordered comparison (`<= high_water_id`) on Instagram shortcodes, which are not lexically orderable — taken literally it never fires, so every top_scan call pages to the ~4-page cap and burns the metered rate budget (invisible because dedupe hides it). Raised independently by Domain Logic (correctness) and Reliability (budget). Fix: make the stop condition membership-in-seen-set or compare on numeric media_id, and store the anchor as a media_id. Top suggestions: classifier `(items, stop_reason)` for mixed 200s; `ok`-family terminal vocabulary (end_of_feed/page_cap_hit/caught_up); CSV-append durability; allowlist cookie guard; enumerate AC1–AC6.

## 2026-07-15 07:23 — planner task v3 (draft)

Revised T1 plan against round-2 review. Resolves the single new blocker: the v2 top_scan stop condition was an ordered `<= high_water_id` comparison on Instagram shortcodes, which are not lexically orderable — it never fires, so every top_scan call pages to the ~4-page cap and burns the metered ~48/6.6min budget (hidden by dedupe). Fix (accepted in full): PRIMARY stop = seen-set membership (stop on first already-seen shortcode; newest-first arrival makes first-known == caught-up, order-tolerant); SECONDARY = numeric `high_water_media_id` watermark (`media_id`/`pk`, monotonic) as belt-and-suspenders. Anchor renamed `high_water_id` -> `high_water_media_id` and typed numeric; identity/dedupe keys off shortcode, ordering off media_id. Added an explicit `pages_fetched == 1` anti-regression acceptance check for a caught-up top_scan to T1.5/T1.7/T1.9. T1.2 extended to confirm pk-vs-shortcode distinction and newest-first ordering. Secondary round-2 suggestions folded in: caught_up (normal) distinguished from stop_signal (abnormal); pages_fetched emitted by the primitive; seen-derivation Open Question; empty/rebuilt-seen edge case as a Risk. No disagreement on any round-2 item.

## 2026-07-15 07:35 — review findings v3 (draft)

Round-3 (final) panel review of the REVISED T1 PLAN (v3), weighted toward the two reviewers who raised/corroborated the round-2 blocker. Verdict: APPROVE. The single round-2 blocker — top_scan stop condition written as an ordered `<= high_water_id` comparison on non-orderable Instagram shortcodes (never fires; every call pages to the ~4-page cap and burns the metered ~48/6.6min budget; hidden by dedupe) — is verified GENUINELY resolved, not name-checked: PRIMARY stop is now order-tolerant seen-set membership (stop on first already-seen shortcode), SECONDARY is a monotonic numeric `high_water_media_id` watermark (`pk`), the anchor was renamed + typed numeric throughout, identity/dedupe keys off shortcode while ordering keys off media_id, and T1.2 was extended to confirm the pk-vs-shortcode distinction and newest-first ordering (with a documented fallback that keeps membership authoritative). Decisive: the fix is enforced by a `pages_fetched == 1` anti-regression acceptance check wired into T1.5/T1.7/T1.9, with `pages_fetched` added to the primitive's emitted contract — the exact failure dedupe would hide now trips a red test. Both originating reviewers (Domain Logic on correctness, Reliability on budget) confirm resolution. All four round-1 blockers remain resolved and un-regressed; no new blocker introduced. Remaining items are non-blocking round-2 carryover suggestions (enumerate AC1–AC6; mock-injected fixture tests; CSV torn-line tolerance; allowlist cookie guard; probe-fixture dev-budget discipline). Terminal review round for the plan — approved to proceed to build.

## 2026-07-15 07:44 — implementation build v1 (draft)

Built the full T1 foundation per approved plan v3: src-layout `ig_media_kit` package with a config loader ($IG_MK_CONFIG + per-call deep-merge), an anonymous curl_cffi client carrying the load-bearing `stop_signal` classifier (401/429→rate_limited, 403→forbidden, 302→login/challenge, 200+challenge-body→stop, fail-closed on unknown) and an auth-cookie-keyed anonymity guard, the shared fetch primitive (user_id resolution + ≤4-page paced pagination + clips normalization with distinct shortcode/numeric media_id + top_scan short-circuit on seen-membership/watermark + deep_resume + emitted pages_fetched), a CSV+YAML store with skip-seen dedupe and durable-first partial persistence (CSV fsync → advance anchors for persisted items only → atomic temp+os.replace state write), a synchronous window that never sleeps, and a bootable FastMCP skeleton (list_reels wired, 3 stubs). 43 offline unit tests pass; quality gate PASS; `python -m ig_media_kit.mcp_server` boots. The T1.2 probe ran LIVE against natgeo and confirmed every relied-upon shape (user_id, 12/page cap, clips+play_count+ig_play_count, video_versions url, next_max_id={media_id}_{user_id}, pk≠shortcode, benign-cookies-only) AND captured a real HTTP 401 stop_signal on page 2 (the metadata rate-limit). One deviation flagged transparently: the probe showed the feed is NOT strictly newest-first (natgeo pins reels; pks_descending==false), which weakens the "first-known==caught-up" premise for pinned accounts — surfaced via a self-describing code marker in fetch._consume_page and a discovered followup, NOT silently redesigned (a redesign risks reintroducing the budget-burn regression the caught-up==1-page invariant guards). Ready for review — recommend multi-perspective-review.

## 2026-07-15 13:20 — review findings v1 (draft)

Multi-perspective panel review of the T1 BUILD (build review, round 1 — distinct from the plan reviews in review-findings-plan.md). Verdict: APPROVE, zero blocking findings; re-ran the suite (43 passed). Every load-bearing invariant the plan called out is implemented and tested: anonymity enforced in code at three choke points (construction/update/every get_api) and even refuses a server-offered auth cookie without crashing; the stop_signal classifier covers the full 401/403/429/302-login/302-challenge/200+challenge family and fails closed to stop(unknown); the sync path passes sleep=None so it never sleeps and is capped at 4 pages; durable-first ordering fsyncs CSV before advancing anchors and writes state via atomic temp+os.replace; and the round-2 shortcode-ordering bug is decisively avoided — top_scan stops on seen-set membership + a numeric high_water_media_id watermark (no `<=` on shortcodes), with `pages_fetched == 1` proven by test. The build's transparently-flagged pinned-reel deviation was assessed SAFE-AS-SHIPPED (the caught-up short-circuit itself is sound; high_water advances monotonically; the store is append+dedupe, so the worst case is temporary, self-healing under-collection — never loss or a politeness regression) and is tracked as accepted debt with a hardening follow-up that must not weaken the short-circuit. Non-blocking: (1) wrap transport-level exceptions/timeout into a typed partial so a network blip doesn't crash list_reels (corroborated by Error Handling + Test Coverage); (2) if a 200-challenge body ever co-resides with real items, persist them before stopping (plan Open Question #5, unconfirmed shape, corroborated); (3) give 5xx its own server_error reason; (4) clear dead imports. Panel: Reliability, Security (LGTM), Error Handling, Data Integrity (LGTM), Domain Logic, Test Coverage, Tech Debt (LGTM), Naming (LGTM).
