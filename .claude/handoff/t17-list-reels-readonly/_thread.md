# Thread — t17-list-reels-readonly

Pivot list_reels to READ-ONLY over the store (CQRS hard split)

## 2026-07-18 10:38 — planner task v1 (draft)

`list_reels` currently couples a read (rank + serve from the store) with a write (a metered IG fetch that deepens the pool toward `scan_depth=90`). That coupling lets an interactive serve call surface cross-channel rate-limit / 401 cooldown errors that originate in the metered path, and leaves the "`list_reels` never sleeps" invariant only half-true (it never sleeps but can still *attempt* IG and fail on a cooldown). The accepted decision (`decisions/architecture/api-contract/2026-07-16-list-reels-is-read-only-over-the-store-cqrs-split.md`) mandates a hard CQRS split: `list_reels` becomes a pure READ-ONLY query over the local store and NEVER hits Instagram on any code path. Analysis moves entirely to the explicit command tools (`start_batch_fetch`; `download_reel`'s >24h re-resolve). Doing it now closes the last interactive metered path and makes the never-sleeps invariant fully true.

9 ordered steps (public-api-change overlay active); status draft — user must approve before an implementation skill consumes it.

## 2026-07-18 10:45 — review findings v1 (draft)

Plan review (iteration 1) of the CQRS read-only pivot — APPROVE, 0 blockers. 10-reviewer backend panel verified all six scrutiny axes: (a) total IG-fetch excision with no escape hatch (grep + zero-IG test guard); (b) readiness gate is coverage-contiguity via `_has_been_analyzed`, cleanly separated from the informational store-count-vs-90 staleness figure; (c) not-analyzed error rides the never-raise envelope with `partial=False`; (d) ranking reused unchanged (per-shortcode dedupe + numeric `media_id`, never positional); (e) zero-IG-request assertion in both cases; (f) README + CLAUDE.md updates present. 5 non-blocking suggestions — top one: `download._error` has no `retryable`/`error_kind` fields (retryability is via `partial`), so the plan's new keys create a contract asymmetry — resolve Open Question #2 as a hard acceptance item. Sound to build once suggestions are noted.

## 2026-07-18 12:39 — implementation build v1 (draft)

`list_reels` (`run_list_reels`) is now a pure READ-ONLY query over the store: resolve params -> validate -> load state -> three-state readiness gate (`_has_been_analyzed`, keyed on coverage evidence not the scan_depth count) -> {typed `not_analyzed` error naming start_batch_fetch | served ranked top-N + deterministic `staleness` block}; ZERO IG requests on every path (poisoned-transport test proves 0 calls on BOTH the served and not-analyzed paths). FORCED DEVIATION discovered during build: `batch._fill_handle` reused `run_list_reels` as its ONLY fetch engine, so the network path was RELOCATED intact into a new `fill.py::run_fill` (command-side primitive) and batch was re-pointed to it (fetch logic byte-identical, all test_batch green) — the plan's own ≥2-module re-plan trigger; recommend the reviewer ratify the extraction as within the CQRS decision's intent. D1: `State.last_analyzed_at` stamped in `write_window` (threaded `now`), legacy YAMLs load None. D2: uniform `error_kind`/`retryable` on list_reels errors AND backfilled onto download `_error`/`_partial`. D3: `signed_url_maybe_expired` over served rows only. `_compose_note` moved with the network path (not dead). 177 pytest pass; offline smoke exit 0. 2 follow-ups (duplicated param helpers -> params.py; dead window.run_window). Ready for review — recommend multi-perspective-review.

## 2026-07-18 12:48 — review findings v2 (draft)

CODE review (iteration 1) of implementation-build v1 — **APPROVE, 0 blockers**; prior PLAN-review findings v1 archived to `_history/review-findings-v1.md`. 12-reviewer backend panel; reviewer independently re-ran the full suite (**177 passed**) and verified all seven scrutiny axes (a–g) by direct source inspection + grep, not by trusting the build note. The FORCED DEVIATION (`fill.py::run_fill` extraction + `batch._fill_handle` re-point) is explicitly **RATIFIED**: verbatim relocation, behavior-preserving on the fetch axis (only delta is additive `now`-threading for `last_analyzed_at`), satisfies both hard invariants (list_reels zero-IG AND batch remains the only coverage-advancing writer). Verified: zero-IG on BOTH served + not-analyzed paths (poison patches `_default_transport`, `hits==0`, list_reels import-clean of fetch/http_client); readiness keys on `_has_been_analyzed` evidence not scan_depth count; uniform `error_kind`/`retryable` across list_reels + download with both frozen snapshots updated deliberately; `staleness` deterministic (served-only) with `signed_url_maybe_expired` over served rows; `last_analyzed_at` backward-compatible None; ranking desc over full deduped pool, non-positional; anonymous-only structurally reinforced. 4 non-blocking suggestions — top: machine-enforce the zero-IG import boundary with a static/AST test (the grep-clean is build-time only, and the two runtime poison tests each exercise one branch). Debts #1 (DUP `_Params`/`_resolve_params`/`_validate` → params.py) + #2 (dead `window.run_window`) endorsed as filed; stale `fetch_gate.py:7` docstring nit. Ready to merge once suggestions are noted.

## 2026-07-18 12:56 — implementation build v2 (draft)

ITERATION on the APPROVED build (review v2, 0 blocking) — absorbed EXACTLY the two concrete, low-risk hardening suggestions and nothing else; no production behavior changed. Prior build v1 archived to `_history/implementation-build-v1.md`. (1) Test Coverage Auditor's top suggestion — machine-enforce the zero-IG import boundary: added `tests/test_import_boundary.py`, a purely STATIC AST guard that resolves `list_reels`' source via `find_spec` (imports only the parent package, never executes the module), walks the ENTIRE module (top-level + nested/lazy imports), and asserts it imports neither `ig_media_kit.fetch` nor `ig_media_kit.http_client` and no `AnonymousClient`/`fetch_window`/`resolve_user_id` symbol — converting the build-time grep into a permanent CI tripwire that also catches a fetch import on an untested third branch (the gap the two single-branch runtime poison tests leave). The single test carries an in-function positive control against `fill.py` (the known violator) proving the detector actually fires, so the suite grows by exactly one (177 → 178). (2) Documentation nit — `fetch_gate.py:7` module docstring corrected `run_list_reels` → `run_fill` (the batch path name after the T17 extraction; docstring-only). Deferred (separate follow-up tickets, NOT done here): DUP `params.py` extraction (#1) and dead `window.py` retire (#2). Quality gate: pytest 178 passed; byte-compile exit 0; offline `probe_smoke` exit 0 with `zero_ig_network True`. Ready for review — low-risk hardening, no new blocking surface.
