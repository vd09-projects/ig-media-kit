# Thread — t2-list-reels-discovery-ranking

Chronological narrative for this scope. Maintained by Skald.

## 2026-07-15 14:22 — planner task v1 (draft)

Task-level breakdown for the first full MCP tool: `list_reels(handle, count, sort_by, min_views, min_duration, max_age_days, scan_depth=90, fresh_fetch=false)` — the interactive, fast, NEVER-BLOCKING surface that returns a channel's top-N reels ranked, filling the store call-by-call up to `scan_depth`. This is Flow A on top of the merged T1 foundation (PR #7): a serve-from-store fast path, a top-check phase (T1 `top_scan`), a deepen phase (T1 `deep_resume`), coverage-segment tracking so a gap opened by a burst of new posts is remembered, partial-on-stop_signal handling, and rank-over-the-full-pool. No downloads. 11 ordered steps (T2.0 field micro-gate → T2.10 live acceptance), base template, no overlays.

## 2026-07-15 14:30 — review findings (plan) v1 (draft)

Plan review round 1, **REQUEST CHANGES**, 4 blockers. Politeness/never-block discipline (T2.3/T2.7) and additive T1-respecting store changes are sound. Blockers: **B1** pinned already-seen reel at feed top short-circuits top-check above genuinely-newer reels (plan silent; T1 build already flagged this as accepted-debt — now due); **B2** B1 collides with the "don't modify T1's frozen top_scan stop condition" constraint — needs a verify-by-pilot micro-gate (T2.0b) against a pinned account before T2.3+ are wired; **B3** `high_water_media_id` must bump to numeric-max of merged items, not the positionally-top (possibly pinned/older) item; **B4** `pool_depth` (count) conflated with coverage contiguity — a gapped >=90 pool can be served as "complete." Corroborated high-signal items: B1 (Domain Logic + Test Coverage), B2 (Domain Logic + Ripple Effect). 4 non-blocking suggestions.

## 2026-07-15 20:10 — planner task v2 (draft)

Revision addressing plan-review round 1 (all 4 blockers + non-blocking suggestions). **B2 resolved by reading merged T1 source:** `fetch._consume_page` TOP_SCAN HARD-STOPS on the first seen/watermarked item (branch b) — B1's skip-not-stop cannot be done T2-side (a pin's low media_id trips the numeric watermark; suppressing it destroys the caught-up 1-page short-circuit), so the plan promotes the bounded pinned-prefix fix the T1 TODO reserved into a new scoped step **T2.4a** (sub-choice b-i), keeping the caught-up==1-page anti-regression green. **B1** fixed by T2.4a (skip-not-stop across ~3-item pin prefix, collect un-seen clips below). **B3** high_water advances to MAX NUMERIC media_id (relies on T1 `write_window`'s existing `max()`), never positional — added to T2.4 AC. **B4** serve-from-store gate now keys on **CONTIGUITY** not raw count (`coverage_contiguous` = single segment to scan_depth/terminal); gapped >=90 pools keep deepening, envelope `complete` == contiguity. Non-blocking folded: x-ig-app-id-only-via-AnonymousClient AC (T2.9), contract-visible questions frozen in T2.1, stop-in-top-check aborts whole call (reserved deepen page unspent, T2.7), numeric gap predicate + phantom-segment guard (T2.5). 12 steps now (added T2.4a).

## 2026-07-15 20:15 — review findings (plan) v2 (draft)

Plan review round 2 (targeted re-review by the 3 round-1 blocking reviewers: Domain Logic, Naming & Clarity, Ripple Effect), **APPROVE**, 0 blockers. **All four round-1 blockers RESOLVED:** **B1** fixed at the predicate level by T2.4a (skip-not-stop across bounded ~3 pin prefix; caught-up re-keyed to "page yielded zero new un-seen clips"; correctness on per-shortcode dedupe). **B2** — the hard contradiction — resolved honestly: verified `_consume_page` TOP_SCAN hard-stops in merged source, lever analysis proved a pure-T2 fix impossible without destroying the caught-up short-circuit, so one bounded `_consume_page` carve-out is promoted out of scope while every other T1 contract stays frozen. **B3** high_water delegated to T1 `store.write_window`'s existing numeric max, monotonic asserted. **B4** `pool_depth` (count) split from `coverage_contiguous` (single segment to scan_depth/terminal); serve-from-store gates on contiguity; `complete` == contiguity; gapped pool made visible not frozen. **New-risk scrutiny (T2.4a budget-burn / caught-up==1-page):** bounded by construction, guarded by mandatory anti-regression test + dedicated review + re-plan escape hatch — tracked risk, not a blocker. Ripple check confirmed the carve-out is fenced against `deep_resume`, page cap, stop classifier, durable-first ordering. 3 non-blocking suggestions (distinct caught_up vs page_cap stop_reason assertion; keep anti-regression test co-located with `_consume_page`; one-line `complete` envelope doc). Plan is APPROVE-ready to build.

## 2026-07-15 17:21 — review findings (build) v1 (draft)

BUILD/code review round 1, **APPROVE**, 0 blockers. All five hard invariants verified present in the actual T2 code AND locked by dedicated tests: **anonymous-only** (`x-ig-app-id` sole-sourced via `AnonymousClient`; `assert_anonymous` on every send/redirect/cookie-update); **politeness** (list_reels never sleeps — `sleep=None` both phases, `test_governor_never_sleeps`; one shared `PageBudget` keeps top-check+deepen ≤ `max_pages_per_call` with ≥1 page reserved for deepen; `partial` is `True iff stop_signal`, so `if not top.partial` aborts the whole call on the first stop_signal, deepen page unspent); **bounded T2.4a carve-out** (only `fetch._consume_page` TOP_SCAN changed — skip ≤`PINNED_PREFIX_BOUND`=3 leading known items then treat next known as the real boundary; `deep_resume` fenced/unchanged; genuinely-caught-up still short-circuits page 1 returning `caught_up` NOT `page_cap`); **non-destructive store** (`is_contiguous` = single segment reaching scan_depth/terminal; serve-from-store gates on contiguity not raw count; a pin can't open a phantom segment; high_water = numeric-max, monotonic); **durable-first ordering** preserved (CSV fsync before state; `save_coverage_segments` reloads so it can't clobber the anchor). **Modified T1 test `test_caught_up_short_circuits_on_membership_page1` judged LEGITIMATE** — the old assertion encoded the exact hard-stop T2.4a intentionally removes; repurposed to guard the genuinely-caught-up anti-regression while the skip-and-collect behavior moved to dedicated `test_pinned_prefix.py` (6 cases). **Unobserved live T2.10 happy-path** ruled an acceptable ship condition with a non-blocking follow-up (pin-float premise + stop-signal both live-proven; the added compose/coverage/ranking is deterministic offline logic; 90 offline tests green) — no code change would result, so not a code blocker. 2 non-blocking suggestions (top-level exception guard vs. soften the no-exception docstring claim; retire/consolidate orphaned `window.run_window`) + 2 FYI (serve-from-store can serve stale >36h signed URLs — a T3/downloader concern; CSV re-read amplification, negligible at ~90 rows).

## 2026-07-15 17:31 — implementation build v1 (draft)

SMALL comment-only iterate — backfilled the two self-describing T2 followup markers with their real GitHub issue numbers (the only point a real issue number enters the code). `src/ig_media_kit/window.py`: the `run_window`-unreferenced / avoid-two-compose-paths-drifting marker now carries `(tracked: #8)`. `src/ig_media_kit/fetch.py`: the `PINNED_PREFIX_BOUND` under-collect-if-IG-raises-pin-cap marker now carries `(tracked: #9)`. Zero logic change; build not re-run; both markers stay self-describing (what + where + why). `pytest -q` -> 90 passed (unchanged). First skald persist of the T2 implementation-build canonical (prior T2 build summary was never captured — only its review was), so this creates it at v1 rather than iterating.
