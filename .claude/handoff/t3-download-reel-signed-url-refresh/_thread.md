# Thread — t3-download-reel-signed-url-refresh

Chronological log of skill activity on this scope. Maintained by Skald.

## 2026-07-15 19:25 — planner task v1 (draft)

Implement `download_reel(shortcode)` — an MCP tool that downloads a single reel's mp4 on demand, transparently re-resolving the fbcdn signed URL when the stored one has aged past its TTL margin, and serving already-downloaded files with zero network. Task-level breakdown T3.1–T3.8 grounded in the existing T1/T2 codebase: a shortcode→owner-handle+row store resolver, a strict no-network cached-hit gate (mirrors the contiguity-gated serve-from-store decision), a TTL-margin freshness decision (~24 h under the ~36 h signed-URL TTL), a shortcode/media_id-anchored owner-feed re-resolve that honors the standing order (never positional/newest-first), a binary redirect-follow CDN download with ftyp-verification and atomic temp+os.replace move, an atomic manifest rewrite of `local_mp4`/`video_url`/`fetched_at`, orchestration in a new `download.py` replacing the `mcp_server.py` stub, and a unit suite + live pilot. Three open questions carried: TTL margin value, whether to persist the refreshed URL, and unknown-shortcode policy.

## 2026-07-16 01:20 — implementation build v1 (draft)

Implemented all 8 subtasks (T3.1–T3.8) on branch `feat/t3-download-reel`, plus the folded-in plan-review fix (ftyp signature at offset 4, not byte 0). New surface: `AnonymousClient.download_cdn` (redirect-following binary CDN GET returning raw bytes) + `CdnResponse` in `http_client.py`; `Store.find_reel` (config∪on-disk shortcode→handle+row) + `Store.update_local_mp4` (atomic full-manifest rewrite, only the target row's local_mp4/video_url/fetched_at change, QUOTE_MINIMAL preserved) in `store.py`; `resolve_reel_url` + `ResolveResult`/`ResolveOutcome` in `fetch.py` (identity-anchored find-by-shortcode/media_id, never positional, with an in-code assertion + standing-order guard test); new `download.py` orchestration (`run_download_reel`) composing the cached-hit no-network gate, 24 h TTL-margin freshness, reuse-or-re-resolve, ftyp-verify + temp-write + os.replace, and the atomic manifest update; `mcp_server.py` stub→real with a try/except backstop so the tool never throws. All CLAUDE.md invariants held: anonymous-only, metered/unmetered asymmetry, x-ig-app-id, redirect-follow, atomic CSV. Tests: 16 new in `tests/test_download.py` (incl. provably-network-free cached path and the not-first-in-feed standing-order guard); `pytest` 106 passed. Live pilot `probe/probe_download.py` written but NOT run (would hit IG; offline unit tests only). One discovered follow-up: a `# TODO:` in `download.py` to fall back to one metered re-resolve when a reused in-margin stored URL fails to download (fbcdn can rotate a signed URL before the margin). Recommends multi-perspective-review.

## 2026-07-15 19:32 — review findings-plan v1 (draft)

Plan review round 1 (7-reviewer backend panel, Reliability/Rate-Limit always-included per project memory). **APPROVE — 0 blocking.** The two load-bearing anchors verified against the T1/T2 code: (a) re-resolve by shortcode/media_id never positional (matches `_item_shortcode`/`_item_media_id` discipline), (b) provably network-free cached path (injected-transport never-called seam). Metered/unmetered split precise; anonymity holds on both new network paths; atomic CSV rewrite faithfully mirrors `_write_state_atomic`. One should-fix (non-blocking): the ftyp check must assert `data[4:8] == b"ftyp"` (the box tag is at offset 4, not byte 0) — both the success metric and T3.5 wording would mislead. All three open questions resolved, none a real blocker: OQ#1 (24 h margin) is a safe in-plan default; OQ#2 (persist refreshed video_url+fetched_at) is confirmed safe AND correct by codebase evidence — nothing reads `fetched_at` (ranking `max_age_days` uses `taken_at`); OQ#3 (unknown-shortcode fallback) is forced to typed-error by the invariants — the per-media info endpoint is dead anonymously, so no IG-wide resolve exists. Plus 4 test additions (pk backstop, not-found-in-budget, close-the-loop persistence, blank-`fetched_at` routing) and a `fetched_at`-overloading comment (corroborated by Data Integrity + Naming).

## 2026-07-16 01:40 — review findings v1 (draft)

Build/code review round 1 (8-reviewer backend panel, Reliability/Rate-Limit always-included per project memory). **APPROVE — 0 blocking.** Every load-bearing CLAUDE.md invariant verified against the actual diff: anonymous-only (`assert_anonymous` + cookie/auth-free `download_cdn`), metered/unmetered asymmetry (CDN GET never sleeps and `allow_redirects=True`; `resolve_reel_url` caps at `max_pages`, classifies every page, stops on first stop_signal, `sleep=None` on the sync path), `x-ig-app-id` on metered calls, atomic full-manifest CSV rewrite (temp+fsync+os.replace, `CSV_COLUMNS` order + `QUOTE_MINIMAL`, all rows/columns preserved, comma-caption quoting round-trips), ftyp verify at byte offset 4, and the STANDING ORDER (identity-anchored match by shortcode/media_id with an in-code assert and a not-first-in-feed positional-trap regression test). No blocking correctness bug, anonymity leak, politeness violation, or store-corruption risk. Three non-blocking suggestions: (1) `test_reresolve_by_numeric_media_id_backstop` (test_download.py:251) never actually exercises the numeric backstop — the feed item's `code` equals the seeded shortcode, so it matches on shortcode; give it a differing `code` with the same `pk`; (2) promote the reused-in-margin-URL 403 fallback `# TODO` (download.py:145) to a tracked task; (3) disambiguate the aged-out envelope (download.py:130) which carries both `error` and `partial=True`. Accepted debt logged: reused-in-margin signed-URL rotation fallback.

## 2026-07-16 01:45 — implementation build v2 (draft)

Iterate mode — surgical, addressed exactly two of the round-1 build-review suggestions and nothing else (prior v1 archived to `_history/implementation-build-v1.md`). (A) `test_reresolve_by_numeric_media_id_backstop` (test_download.py) now gives the target feed item a DIFFERENT `code` (`"RENAMED"`) but the SAME numeric `pk=555` as the seeded shortcode (`"OLDCODE"`), so no shortcode match is possible and only the numeric media_id backstop (`pk == media_id`) can select it — a real assertion of the non-positional identity-by-media_id behavior, plus a premise-lock (`all(code != "OLDCODE")`) and a not-`items[0]` check. (B) In `download.py run_download_reel`, the aged-out / not-found-in-budget branch no longer sets `partial=True` — it returns a clean non-partial typed error, disambiguated from the stop_signal `_partial` case (metered cooldown, retryable) so a retryability consumer can tell the two apart; the two tests now pin both envelope shapes (`partial is False` for aged-out, `partial is True` + `stop_reason` for stop_signal). The reused-in-margin-URL 403 fallback `# TODO` at download.py was deliberately left AS-IS (tracked as a separate follow-up). All CLAUDE.md invariants held (anonymous-only, typed envelopes, politeness). `uv run pytest -q`: 106 passed, 0 failed. Ready for review.
