# Thread — t5-ship-mcp-server-packaging

Chronological record of skald-persisted artifacts for this scope. Newest at bottom.

## 2026-07-16 07:57 — planner task v1 (draft)

The four tools (`list_reels`, `download_reel`, `start_batch_fetch`, `get_batch_status`) are already built, tested, and merged as separate modules, but nothing yet assembles them into one runnable, packaged, documented FastMCP server a fresh cloner can start. A skeleton `mcp_server.py` exists but ships a leftover `top_reels` stub, mis-names the batch tool (`batch_fetch`), loads config per-call instead of sharing one context, and never calls `resume_pending_jobs` at startup (the accepted daemon-thread-plus-explicit-resume decision). This ticket is the capstone: freeze the MCP tool surface as the public contract, package + document the fresh-clone path, convert the existing `product_type` filter into a demonstrated extension switch, and deliver a smoke harness that exercises the wiring without hitting IG live (the live run is deferred to pilot tickets #10/#14 because this IP is subject to IG's escalating cooldown).

Task-depth plan, 6 reviewable slices (server wiring; four-tool surface freeze; product_type switch + stub; packaging+config; README; offline smoke harness), public-api-change overlay active. Status: draft — awaiting approval before an implementation skill consumes it.

## 2026-07-16 08:04 — review findings v1 (draft)

Plan review (iteration 1) of planner-task v1 by a 10-reviewer panel. Overall REQUEST CHANGES. Plan is strong and complete — all 5 acceptance criteria covered, invariants enumerated, and the deferral of the live IG smoke to pilots #10/#14 (fixture/dry-run harness + documented opt-in live procedure) is endorsed as correct against the escalating-cooldown invariant. Two blocking plan-level defects, both cheap: (1) step 2 assumes the never-raise envelope is "already present" on all four tools, but `list_reels` (mcp_server.py:24-57) has no try/except — an implementer would ship the frozen surface with an unmet never-raise AC; (2) step 1 leaves a per-call `config_path` path that can re-key the process-wide `FetchGate` off a different `store_dir`, defeating the one-gate invariant. Top suggestions: make product_type dispatch observable so its routing test is writeable; exercise a simulated 401 through the wired surface; reuse load_config's existing $IG_MK_CONFIG precedence (config.py:107, already implemented). Status: draft — awaiting approval.

## 2026-07-16 08:09 — planner task v2 (draft)

Iteration on plan review round 1 (findings v1, REQUEST CHANGES). Prior planner-task v1 archived to _history/planner-task-v1.md. The revision opens with a Review Response section acknowledging all 8 review items (2 blocking, 6 non-blocking) — none rejected, two refined. Blocking-1: list_reels (mcp_server.py:24-57) had no never-raise envelope; step 2 now wraps it in the shared typed envelope and the contract test asserts every tool returns a dict (never raises) when its run_* throws. Blocking-2: a per-call config_path could re-key get_gate off a different store_dir and split the one-process FetchGate; step 1 now resolves one gate from server context, forbids re-keying, constrains config_path to a store-compatible/test-only override (divergent store rejected), and asserts resume completes before mcp.run() with the daemon sharing the same gate instance. Non-blocking folded in: observable product_type dispatch (named registry + typed SkipReason + tracked follow-up marker), a simulated mid-fetch 401 through the wired surface, Open Question #3 resolved (config.py:107 already implements the precedence — step 4 is verify-only), operator-facing resume_pending_jobs docs, and the smoke callback sink injected as a test seam rather than loosening the SSRF guard. Status: draft — awaiting re-review before an implementation skill consumes it.

## 2026-07-16 08:16 — review findings v2 (draft)

Plan review (iteration 2, targeted) of planner-task v2 by the two round-1 blocking reviewers (API & Contract; Concurrency & State Safety) with their prior findings attached, plus a full invariant-compliance pass. Prior findings v1 archived to _history/review-findings-v1.md. Overall APPROVE, 0 blocking. Both round-1 blockers verified RESOLVED against live source: (1) never-raise — mcp_server.py:51-56 confirmed still un-wrapped (bare return run_list_reels) while the other three carry except-Exception backstops; plan v2 makes the wrap explicit Step 2 work + upgrades the contract test to a behavioral dict-return-on-throw across all four tools. (2) config_path gate split — plan's resolve-once-from-server-context + reject-divergent-store_dir-config_path fix closes the concern and the real underlying config/store-divergence hazard. One non-blocking accuracy carry-forward: fetch_gate.py:187-215 get_gate is actually an argument-ignoring module singleton (returns the same instance regardless of args), NOT a store_dir-keyed map as the plan (lines 45/69) and CLAUDE.md describe — so the gate cannot literally be split by config_path today, and the plan's gate-identity test assertion is vacuous; the prescribed guard is correct regardless, but the wording should be fixed and the gate test should lean on the rejection assertion. All six round-1 non-blocking suggestions confirmed folded in; all project invariants honored (politeness now verified through the wired surface). Status: draft — approves the plan to build.

## 2026-07-16 11:21 — implementation build v1 (draft)

Built the T5 capstone from planner-task v2 (all 5 slices), assembling the four merged tools (list_reels, download_reel, start_batch_fetch, get_batch_status) into one runnable/packaged/documented FastMCP server. mcp_server.py was rewritten around a frozen ServerContext (one config + one Store + the single process-wide FetchGate) that every tool reads — no per-call load_config, no per-call gate resolution; the stale top_reels stub is gone, batch_fetch is renamed to start_batch_fetch, and list_reels now carries the same typed never-raise envelope as the other three. main() parses --config and REUSES load_config's precedence (explicit > $IG_MK_CONFIG > ./config.yaml), and a separated startup() installs the context and calls resume_pending_jobs EXPLICITLY before mcp.run(). The one-gate invariant is enforced by rejecting a config_path whose store_dir diverges from the server context (typed envelope, never-raise) — the guard asserts divergent-store REJECTION rather than object identity, because get_gate is an argument-ignoring process singleton and identity is vacuous. fetch.py's product_type drop became an observable switch: a named _PRODUCT_HANDLERS registry + typed SkipReason (UNSUPPORTED_PRODUCT_TYPE / MALFORMED) in a NormalizeResult, with a disabled image demonstrator stub carrying a self-describing follow-up marker (no minted issue number); normalize_item stays a byte-for-byte-compatible wrapper and the store stays clips-only. config.yaml gained the batch politeness block; README was fully rewritten (install/configure/run, startup job adoption, the four tools + when-to-use, ~48/6.6-min escalating politeness, ~36h signed-URL TTL/24h margin, Burner rejected linking the report section, and the deferred-live/fixture smoke). A new probe/probe_smoke.py drives the wired surface offline (list -> download against a stubbed ftyp body -> batch daemon fill + a REAL localhost callback sink injected as a poster test seam -> status), plus a simulated mid-fetch 401 asserting partial + stop_reason + stop-on-first-401, all under a guard that hard-fails if any code path builds a real transport (zero IG network enforced); the production https/SSRF callback guard is untouched, and the live run is opt-in-gated and DEFERRED to pilots #10/#14 (not faked green). Quality gate PASS: pytest 162 passed (145 baseline + 17 new), deterministic across 3 runs; fresh-clone pip install . -> ig-media-kit --help exits 0 with four tools; the server boots with the resume-before-serve line. Two discovered follow-ups reported (the product_type stub seam marker; and that the sync tools share the gate object but do not acquire() it — a non-sleeping cooldown check for list_reels/download is a real design question T5 explicitly deferred). Status: draft — Ready for review, recommends multi-perspective-review.

## 2026-07-16 11:27 — review findings v3 (draft)

BUILD / code review (iteration 1) of implementation-build v1 against the working tree on feat/t5-ship-mcp-server — a fresh review of the IMPLEMENTED build, not a plan re-review; prior findings v2 (a plan review) archived to _history/review-findings-v2.md. 10-reviewer backend panel (API & Contract, Concurrency & State Safety, Error Handling, Security & Trust, Test Coverage, Backward Compatibility, Developer Experience, Documentation, Tech Debt, Naming). Overall APPROVE, 0 blocking. Both round-1 plan blockers confirmed closed IN CODE against verified source: list_reels now carries the typed never-raise envelope (test_all_four_tools_never_raise_when_run_throws), and the one-gate guard is the load-bearing divergent-store_dir REJECTION (ContextMismatch -> typed envelope; test asserts envelope + untouched context), correctly framed around config/store divergence sharing the arg-ignoring get_gate singleton rather than vacuous object identity. Verified: all run_* signatures accept the new store=/deps=/client= kwargs; all three changed modules byte-compile clean; fetch.py imports Enum/dataclass/Callable (product_type switch imports resolve). Invariants all honored — resume_pending_jobs before mcp.run() (order-spy), observable product_type switch shipping clips-only with a non-positional shuffled-feed watermark test, zero-IG-enforced smoke, SSRF/callback guard intact (localhost delivery via injected poster seam, validate_callback_url unchanged), live pass legitimately deferred to #10/#14 (printed procedure, not faked green). One real non-blocking tension raised by both Concurrency + Tech Debt: sync tools share the gate object but do not acquire() it, so list_reels/download can hit IG during an active batch cooldown — NOT a T5 regression, explicitly plan-scoped Out of Scope, honestly disclosed as build follow-up #2; recommend filing it (and the non-clip-enablement stub) as real tickets. Caveat: pytest/mcp not installed in the review sandbox, so the "162 passed" claim was not independently re-executed — findings rest on source analysis + clean byte-compile. Status: draft — APPROVE to proceed.
